# Shadow Logic Package

`typemoonaddon-shadow-logic-1.21.1-<version>.jar` 是附属自有的影术运行包，包含影术服务、影之束缚、影带实体与渲染、模式 payload 和附属效果。

主 JAR 负责注册 ID、玩家 attachment、魔术定义、进度、公共事件以及黑泥/从者兼容边界。两个 JAR 必须同版本安装。

该拆包不包含 Type Moon World 类，不声明 Mixin，也不依赖本体实现包。
